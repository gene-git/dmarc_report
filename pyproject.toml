[tool.poetry]
name = "dmarc_report"
version = "4.0.0"
description = "Generate a human readable DMARC report from 1 or more standard xml email reports."
authors = ["Gene C"]
license = "MIT"
readme = "README.rst"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
